//: Playground - noun: a place where people can play

import UIKit

/**
 赋值运算符：=
 a = b; 表示把 b 的值赋给 a，即用 b 的值来初始化或更新 a 的值
 注意：赋值运算符(=)两边必须有一个空格
 */
let b = 10;
var a = 5;
print(a); // 5

a = b;
print(a); // 10

